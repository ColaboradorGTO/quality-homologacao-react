/* import * as yup from "yup";

export const schema = yup.object().shape({
    produtoIncluir: yup.string()
        .required('Produto Obrigatório')
        .test('not-empty', 'Produto não pode ser vazio', value => value && value.trim() !== ""),
    // empresaDestinoIncluir: yup.string()
    //     .required('Loja Destino Obrigatório')

});
 */